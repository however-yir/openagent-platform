# ForgePilot Studio 仓库品牌信息（Repository Profile）

## 推荐仓库名称

`forgepilot-studio`

## 推荐仓库描述（GitHub Description）

智能工程执行工作台，支持 Agent 任务编排、运行时沙箱、审计回放、多模型接入与私有化部署。

## 推荐 GitHub Topics

`ai-agent` `agentic-engineering` `developer-tools` `llm` `workflow-orchestration` `self-hosted` `python` `react`

## 设置命令示例（GitHub CLI）

```bash
gh repo edit however-yir/forgepilot-studio \
  --description "智能工程执行工作台，支持 Agent 任务编排、运行时沙箱、审计回放、多模型接入与私有化部署。" \
  --add-topic ai-agent \
  --add-topic agentic-engineering \
  --add-topic developer-tools \
  --add-topic llm \
  --add-topic workflow-orchestration \
  --add-topic self-hosted \
  --add-topic python \
  --add-topic react
```

## Logo 使用建议

- README 顶部：`docs/assets/forgepilot-logo.svg`
- 社交分享图（可选）：`docs/assets/forgepilot-social-preview.png`
