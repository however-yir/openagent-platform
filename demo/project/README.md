# ForgePilot Demo Project

This demo folder is used to showcase the task lifecycle:

- Create task from template.
- Execute safe code changes.
- Run verification commands.
- Generate report payload.

Use `demo/data/sample-task-template.json` as the initial template in demos.
